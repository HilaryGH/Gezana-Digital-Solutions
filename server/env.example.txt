# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB
MONGO_URI=mongodb://localhost:27017/gezana

# JWT
JWT_SECRET=your_jwt_secret_key_here

# Session Secret
SESSION_SECRET=your_session_secret_here

# Client URL
CLIENT_URL=http://localhost:5173

# Public API origin (production / Render — no trailing slash)
SERVER_URL=https://gezana-api-m8u7.onrender.com
# Optional aliases used by some routes: BASE_URL, API_PUBLIC_URL, FILE_BASE_URL
# Frontend (Vite) production build override:
# VITE_API_BASE_URL=https://gezana-api-m8u7.onrender.com/api

# Social Authentication
# Google OAuth - Get from: https://console.developers.google.com/
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Facebook OAuth - Get from: https://developers.facebook.com/
FACEBOOK_APP_ID=your_facebook_app_id
FACEBOOK_APP_SECRET=your_facebook_app_secret
# Production callbacks (must match Google Cloud / Meta app settings)
GOOGLE_REDIRECT_URI=https://gezana-api-m8u7.onrender.com/auth/google/callback
FACEBOOK_CALLBACK_URL=https://gezana-api-m8u7.onrender.com/auth/facebook/callback

# Email Configuration (Resend API - recommended for production)
# Why not SMTP? Some hosts (including Render) may block outbound SMTP ports (465/587),
# which causes ETIMEDOUT/ECONN errors. API email uses HTTPS (443), which is usually allowed.
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxx
EMAIL_FROM=HomeHub Digital Solutions <onboarding@resend.dev>
# Optional alias used by email service:
# RESEND_FROM_EMAIL=HomeHub Digital Solutions <onboarding@resend.dev>
# BOOKING_AWAIT_NOTIFICATIONS=1  # await email before HTTP response (serverless or if mail never sends)

# Twilio Configuration (for WhatsApp)
# Sign up at: https://www.twilio.com/
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+1234567890
TWILIO_WHATSAPP_NUMBER=whatsapp:+1234567890

# Chapa Payment (if applicable)
CHAPA_SECRET_KEY=your_chapa_secret_key

# File Upload
MAX_FILE_SIZE=5242880

