/** Production API origin (no path). Must match Render Web Service URL. */
export const PUBLIC_API_ORIGIN = "https://gezana-api-m8u7.onrender.com";

/** Axios / JSON API base (includes /api). */
export const PUBLIC_API_BASE = `${PUBLIC_API_ORIGIN}/api`;
